# JSP view resolution
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp

# Logging
logging.level.org.springframework.web=INFO

# JPA / H2 (keep from your existing config)
spring.jpa.show-sql=true
spring.h2.console.enabled=true